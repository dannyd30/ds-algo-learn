/**
 * Heap
 * Binary Heap
 * 
 * Max Heap -> a tree where the value of children is always smaller than the parent
 * Min Heap -> a tree where the value of children is always greater than the parent
 * 
 * Binary Heap -> Max/Min heap with two children
 * 
 * lookup - O(n)
 * insert/delete - O(log n)
 * 
 * uses: data storage, priority queue, sorting algo
 * 
 * MEMORY HEAP IS NOT HEAP DS
 * 
 * remember if yu need all values above a certain number yu can find that number node and take the values above and to side of it
 * 
 * HEAP INSERTION IS ALWAYS LEFT TO RIGHT
 */